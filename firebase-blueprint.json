{
  "entities": {
    "ProjectionState": {
      "title": "Projection State",
      "description": "The current state of the Bible projection, synchronized across all instances.",
      "type": "object",
      "properties": {
        "bookId": {
          "type": "string",
          "description": "The ID of the current book."
        },
        "chapter": {
          "type": "integer",
          "description": "The current chapter number."
        },
        "verseIndex": {
          "type": "integer",
          "description": "The index of the currently projected verse."
        },
        "translation": {
          "type": "string",
          "description": "The current translation code."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the state was last updated."
        },
        "updatedBy": {
          "type": "string",
          "description": "The UID of the user who last updated the state."
        },
        "highlightedWords": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of words to highlight in the projection."
        }
      },
      "required": ["bookId", "chapter", "verseIndex", "translation", "updatedAt", "updatedBy"]
    },
    "User": {
      "title": "User Profile",
      "description": "User profile data stored in Firestore.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "photoURL": {
          "type": "string",
          "description": "The user's profile photo URL (Base64 or external)."
        },
        "isPro": {
          "type": "boolean",
          "description": "Whether the user has Pro status."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the profile was last updated."
        }
      },
      "required": ["displayName", "updatedAt"]
    }
  },
  "firestore": {
    "/projection/current": {
      "schema": "ProjectionState",
      "description": "The single document holding the current projection state."
    },
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile document."
    }
  }
}
